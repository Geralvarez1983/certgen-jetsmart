[phases.setup]
nixPkgs = ["libreoffice", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install flask openpyxl python-docx gunicorn"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
